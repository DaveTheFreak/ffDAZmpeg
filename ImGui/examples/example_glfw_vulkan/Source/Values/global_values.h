#pragma once

struct global_values
{
    /** Current progress of the conversion. */
    float current_conversion_progression;

    /** If the app is currently converting images. */
    bool is_currently_converting;
};

